<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>GIF Search Results</title>
</head>
<body>
<h1>GIF Search Results</h1>
{% if gifs is empty %}
    <p>No GIFs found.</p>
{% else %}
    <ul>
        {% for gif in gifs %}
            <li><img src="{{ gif.url }}" alt=""></li>
        {% endfor %}
    </ul>
{% endif %}
</body>
</html>
